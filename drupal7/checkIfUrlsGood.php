<?php
require_once('utilities.php');

$urls = [];

$good = [];
$bad = [];
foreach ($urls as $url) {
    if (isUrlGood($url, 30, true)) {
        array_push($good, $url);
    } else {
        array_push($bad, $url);
    }
}
echo "========================================================================";
echo "=============================Good=======================================";
echo "========================================================================";
print_r($good);
echo "========================================================================";
echo "=============================Bad========================================";
echo "========================================================================";
print_r($bad);
